import { Token, OAuthProviderConfig } from '../index';
declare type TokenGrantAudiences = 'OPERATE' | 'ZEEBE' | 'OPTIMIZE' | 'TASKLIST' | 'CONSOLE';
export declare class OAuthProviderImpl {
    private static readonly defaultTokenCache;
    private static readonly getTokenCacheDirFromEnv;
    private cacheDir;
    private zeebeAudience;
    private authServerUrl;
    private clientId;
    private clientSecret;
    private useFileCache;
    tokenCache: {
        [key: string]: Token;
    };
    private failed;
    private failureCount;
    private userAgentString;
    private audience;
    private expiryTimer?;
    constructor({ 
    /** OAuth Endpoint URL */
    authServerUrl, 
    /** OAuth Audience */
    audience, clientId, clientSecret, userAgentString }: OAuthProviderConfig);
    close(): void;
    getToken(audience: TokenGrantAudiences): Promise<string>;
    private debouncedTokenRequest;
    private safeJSONParse;
    private fromFileCache;
    private toFileCache;
    private isExpired;
    private startExpiryTimer;
    private getCacheKey;
    private getCachedTokenFileName;
    private getAudience;
}
export {};
