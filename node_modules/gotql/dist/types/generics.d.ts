export declare namespace GotQL {
    type Nullable<T> = T | null;
    enum ExecutionType {
        QUERY = "query",
        MUTATION = "mutation"
    }
    type Response = {
        data: object;
        statusCode: number;
        message: string;
        endpoint: string;
        options: string;
    };
}
//# sourceMappingURL=generics.d.ts.map